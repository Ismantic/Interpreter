# Qwen translation project — pinned deps (Python 3.10)
# torch + vllm are CUDA builds: install torch/vllm FIRST per your CUDA version
# (see https://pytorch.org and https://docs.vllm.ai), then the rest.
torch==2.11.0
vllm==0.20.2
transformers==4.57.6
peft==0.19.1
trl==1.4.0
accelerate==1.13.0
datasets==4.8.5
sacrebleu==2.6.0
unbabel-comet==2.2.7
numpy==2.2.6
